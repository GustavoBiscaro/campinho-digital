/*
Complete o código abaixo para que ele seja capaz de exibir na tela a tabuada de multiplicação do número 6 (de 0x6 até 10x6):

Aqui está o código completo para exibir a tabuada de multiplicação do número 6 de 0x6 até 10x6 usando um loop `faca...enquanto`:


programa
{
  funcao inicio()
  {
    inteiro contador = 0
    inteiro resultado

    escreva("Tabuada de 6\n\n")

    faca
    {
      resultado = contador * 6
      escreva(contador + " x 6 = " + resultado + "\n")
      contador++
    }
    enquanto (contador <= 10)
  }
}
```

Neste código, usamos um loop `faca...enquanto` para calcular e exibir a tabuada de multiplicação do número 6, indo de 0x6 até 10x6. O loop continuará até que o contador seja maior do que 10.
*/